  import type { NextPage } from 'next';
import Head from 'next/head';

const Home: NextPage = () => (
  <div>
    <Head>
      <title>School</title>
      <meta name="description" content="Bitsidy.com is a crypto wallet and payment gateway service"/>
    </Head>
  </div>
);

export default Home;
